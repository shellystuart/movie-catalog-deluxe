<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Movies Index</title>
  </head>
  <body>
    <h1> Movies </h1>
    <table>
      <tr>
        <td>Title</td>
        <td>Year</td>
        <td>Rating</td>
        <td>Genre</td>
        <td>Studio</td>
      </tr>
        <% @movies.each do |movie| %>
          <tr>
            <td><a href="/movies/<%= movie["id"] %>"><%= movie["title"] %></a></td>
            <td><%= movie["year"] %></td>
            <td><%= movie["rating"] %></td>
            <td><%= movie["genre"] %></td>
            <td><%= movie["studio"] %></td>
          </tr>
        <% end %>
    </table>
  </body>
</html>
